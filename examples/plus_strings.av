# plus_strings.av
# Demonstrates string concatenation with + and concat builtin

let a = "hello" in
let b = " world" in
@/tmp/plus_strings.txt {"
    Result: {a + b}
    Also concat: {(concat a b)}
    Repeated: { (join [a,b,a] " - ") }
"}
