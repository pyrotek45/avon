# Example: Secrets Management
#
# This example demonstrates how to use `env_var_or` to safely handle secrets.
# The secret is read from the environment with a default for testing.
#
# Usage:
#   export API_KEY="secret-123"
#   avon eval examples/secrets_test.av
#
# Or just run without export to use the default test value.

let api_key = env_var_or "API_KEY" "test-api-key-default" in
@config.yml {"
api_key: {api_key}
debug: false
"}
