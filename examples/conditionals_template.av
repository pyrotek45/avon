# conditional expressions inside templates
let name = "" in
let item_count = 0 in
@tmp/cond_output.txt {"
    Hello {if name == "" then "stranger" else name},
    You have {if item_count > 0 then item_count else 0} items
"}
